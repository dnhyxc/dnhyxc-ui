export declare class ModelContainer {
    width: number | string;
    maxWidth?: number | string;
    backgroundImage?: string;
    backgroundColor?: string;
    constructor(options: ModelContainer);
}
